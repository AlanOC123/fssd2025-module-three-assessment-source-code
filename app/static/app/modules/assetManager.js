const _ASSET_ORIGIN = document.querySelector(
    "meta[name='asset-origin']"
)?.content;

if (!_ASSET_ORIGIN) throw new Error(`Content Path not found. ${_ASSET_ORIGIN}`)

const getUrl = (key) => {
    
}

export default {

}